/buck-out
**/node_modules
**/dist
**/*.tsbuildinfo

# pnpm workspace — ignore npm lockfiles so they are never committed accidentally
package-lock.json

# JVM compiled artifacts
**/*.class
**/*.jar
**/*.war
**/*.ear

# Python bytecode
**/__pycache__/
**/*.pyc

# Release build artifacts (scripts/build_php_artifact.sh)
api-php-*.tar.gz
api-php-admin-*.tar.gz

# macOS/editor noise
.DS_Store
*.swp
