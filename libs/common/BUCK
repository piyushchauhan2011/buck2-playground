# Shared library - depends on libs/utils, used by multiple domains
genrule(
    name = "common",
    out = "common.txt",
    cmd = "_utils=$(location //libs/utils:utils); printf 'shared common lib\\nversion: 2.0.0\\n' > $OUT",
    visibility = ["//domains/..."],
)
